import { PageSection } from '../components/common/PageSection'

export function LoginPage() {
  return (
    <PageSection
      title="Authentication"
      description="JWT signup/login integration with backend APIs will be completed in Phase 7."
    >
      <div className="placeholder-card">Auth forms and user session state will appear here.</div>
    </PageSection>
  )
}
